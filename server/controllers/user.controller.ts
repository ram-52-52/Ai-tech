import { Request, Response } from "express";

export const getProfile = (_req: Request, res: Response) => {
  // Placeholder for future GET /api/user/profile
  res.status(501).json({ message: "Not implemented yet" });
};

export const updateProfile = (_req: Request, res: Response) => {
  // Placeholder for future PUT /api/user/profile
  res.status(501).json({ message: "Not implemented yet" });
};
