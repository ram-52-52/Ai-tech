## Packages
framer-motion | Smooth page transitions and layout animations
recharts | Visualizing trend data on the dashboard
date-fns | Formatting dates nicely
lucide-react | Beautiful icons for the UI
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes

## Notes
The backend provides endpoints for blogs and trends.
We need to handle the generation process which might take a few seconds, so good loading states are crucial.
The design should be "clean minimal, dashboard style".
